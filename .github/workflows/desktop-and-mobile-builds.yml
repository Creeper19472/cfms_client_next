name: Desktop and Mobile Builds (Windows and APK)

on:
  # Runs on push to any of the below branches
  push:
    # branches:
    #   - master
    #   - dev
    tags:
      - "v*.*.*"

  # # Runs on pull request events that target one of the below branches
  # pull_request:
  #   branches:
  #     - master
  #     - dev

  # Allows you to run this workflow manually from the Actions tab of the repository
  workflow_dispatch:

env:
  # https://flet.dev/docs/publish#versioning
  # BUILD_NUMBER: 1
  # BUILD_VERSION: 1.0.0

  # Python version to use
  PYTHON_VERSION: 3.14.3
  
  # Ensures Python uses UTF-8 encoding by default
  PYTHONUTF8: 1
  
  # Disables rich text formatting in Flet CLI output
  FLET_CLI_NO_RICH_OUTPUT: 1
  
  # Disables progress bars when using UV
  UV_NO_PROGRESS: 1

  # APP name, used for naming the release artifact for mobile builds
  APP_NAME: "cfms-client-next"

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        submodules: 'recursive'

    - name: Setup Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Setup Environment
      run: |
        python -m pip install --upgrade pip
        python -m pip install uv
        uv sync --dev --upgrade

    - name: Determine Release Type
      id: release_type
      run: |
        $isPrerelease = python scripts/get_release_type.py
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Failed to determine release type"
          exit 1
        }
        $channelName = python scripts/get_channel_name.py
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Failed to determine channel name"
          exit 1
        }
        echo "prerelease=$isPrerelease" >> $env:GITHUB_OUTPUT
        echo "channel=$channelName" >> $env:GITHUB_OUTPUT
      shell: pwsh

    - name: Extract Changelog Notes
      id: changelog
      run: python scripts/get_changelog_notes.py "${{ github.ref_name }}"
      shell: pwsh

    - name: Flet Build Windows
      run: |
        uv run flet build windows --yes -v --no-rich-output

    # - name: Upload Windows Artifact
    #   uses: actions/upload-artifact@v4.3.4
    #   with:
    #     name: windows-build-artifact
    #     path: build/windows
    #     if-no-files-found: warn
    #     overwrite: false

    - name: Archive Release
      uses: thedoctor0/zip-release@0.7.6
      with:
        type: 'zip'
        filename: 'cfms-cli-${{ github.ref }}-windows.zip'
        path: 'build/windows'
        exclusions: '*.git* /*node_modules/* .editorconfig'

    - name: Release
      uses: softprops/action-gh-release@v2
      if: github.ref_type == 'tag'
      with:
        files: |
          cfms-cli-${{ github.ref }}-windows.zip
        # overwrite_files: true
        generate_release_notes: true
        body: |
          ${{ steps.changelog.outputs.changelog_notes }}
          <!-- channel: ${{ steps.release_type.outputs.channel }} -->
        prerelease: ${{ steps.release_type.outputs.prerelease == 'true' }}

  build-apk:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        submodules: 'recursive'

    - name: Setup Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Setup Environment
      run: |
        python -m pip install --upgrade pip
        python -m pip install uv
        uv sync --upgrade

    - name: Determine Release Type
      id: release_type
      run: |
        isPrerelease=$(python scripts/get_release_type.py)
        if [ $? -ne 0 ]; then
          echo "Failed to determine release type"
          exit 1
        fi
        channelName=$(python scripts/get_channel_name.py)
        if [ $? -ne 0 ]; then
          echo "Failed to determine channel name"
          exit 1
        fi
        echo "prerelease=$isPrerelease" >> $GITHUB_OUTPUT
        echo "channel=$channelName" >> $GITHUB_OUTPUT

    - name: Extract keystore
      run: |
        mkdir -p ./build/flutter/android/app
        echo ${{ secrets.ENCODED_KEYSTORE }} | base64 -di > ./build/flutter/android/app/keystore.jks

    - name: Flet Build APK
      run: |
        uv run flet build apk --yes -v --no-rich-output --android-signing-key-store keystore.jks --android-signing-key-alias key0 --android-signing-key-store-password ${{ secrets.FLET_ANDROID_SIGNING_KEY_STORE_PASSWORD }} --android-signing-key-password ${{ secrets.FLET_ANDROID_SIGNING_KEY_PASSWORD }}

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4.3.4
      with:
        name: apk-build-artifact
        path: build/apk
        if-no-files-found: warn
        overwrite: false

    - name: Release
      uses: softprops/action-gh-release@v2
      if: github.ref_type == 'tag'
      with:
        files: |
          build/apk/${{ env.APP_NAME }}.apk
          build/apk/${{ env.APP_NAME }}.apk.sha1
        # overwrite_files: true
        # generate_release_notes: true
        prerelease: ${{ steps.release_type.outputs.prerelease == 'true' }}